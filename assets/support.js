(()=>{const form=document.getElementById('ipw-support-form');if(!form)return;const status=document.getElementById('support-status');const submit=document.getElementById('submit-support');const endpoint='https://ipw-marketplace-webhook.onrender.com/api/support';form.addEventListener('submit',async e=>{e.preventDefault();status.className='form-status';status.textContent='';if(!form.reportValidity())return;submit.disabled=true;submit.textContent='Submitting…';const data=Object.fromEntries(new FormData(form).entries());try{const res=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});let body={};try{body=await res.json()}catch{}if(!res.ok){throw new Error(body.error||'request_failed')}form.reset();status.className='form-status ok';status.textContent=body.reference?`Request received. Reference ${body.reference}.`:'Request received.';}catch(err){status.className='form-status error';status.textContent='We could not submit your request right now. Please try again shortly.';}finally{submit.disabled=false;submit.textContent='Submit request';}});})();